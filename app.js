class Ship{
 constructor(length, hits, isSunkk){
   this.length = length;
   this.hits = hits;
   this.isSunkk = isSunkk;
 }

 hit(){
    return this.hits + this.hits;
 }

 isSunk(){
    return this.length + this.hits;
 }

 is
}